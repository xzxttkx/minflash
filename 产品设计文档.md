《“极简闪记”（暂定名）产品设计文档 (PRD)》

一、 产品概述

产品名称：极简闪记（暂定）

运行平台：Android（安卓系统）

产品定位：一款主打“秒级录入”与“强效防遗忘”的极简待办提醒工具。

核心理念：今日事今日毕。摒弃复杂的分类和沉重的历史包袱，让用户专注于当下的任务。

二、 核心功能说明

1\. 极简录入模块

文本输入：提供直观的输入框，支持快速打字输入事件内容。

语音输入（核心亮点）：支持语音快捷输入。系统需具备语音转文字及自然语言时间解析功能（例如用户说“下午五点买洗衣粉”，系统自动提取“下午五点”作为提醒时间，提取“买洗衣粉”作为事件内容）。

2\. 强提醒与交互模块

多渠道提醒：到达设定时间后，触发系统级通知栏推送，并伴随闹铃声或手机震动。

摇一摇关闭（核心亮点）：在提醒响起时，用户只需摇晃手机即可关闭闹铃/震动。此操作仅停止物理提醒，不删除任务记录。

通知栏快捷修改：在系统下拉通知栏的提醒卡片上，提供“修改时间”的快捷按钮，用户无需打开App即可直接调整提醒时间。

3\. 任务管理模块

单一列表：首页仅保留一个简单的大列表，所有任务按时间顺序从上到下排列，不提供任何分类、标签或文件夹功能。

手动完成：用户完成任务后，需在App内手动滑动或点击删除该任务。

三、 业务逻辑与数据规则（开发重点关注）

1\. 任务生命周期（核心规则）

当日有效：所有创建的任务仅在“当天”有效。

午夜清空：每日凌晨 00:00，系统自动清空/消失所有未手动删除的历史任务。

无过期状态：未做完的任务不会标红，也不会自动顺延到第二天，而是直接消失，确保用户第二天打开App时是一个全新的、无压力的列表。

2\. 状态流转

新建 -> 进入当日列表 -> 触发提醒。

提醒中 -> 摇一摇 -> 停止声音/震动 -> 任务仍保留在列表中。

提醒中 -> 通知栏修改时间 -> 重新设定倒计时。

完成 -> 用户手动删除 -> 任务从列表移除。

跨天 -> 凌晨0点 -> 系统自动清理残留任务。

四、 技术关注点提示（给开发小哥的备注）

语音解析：需要接入语音识别API，并配合NLP（自然语言处理）库来精准提取时间实体。

后台保活与通知：作为强提醒工具，需重点处理安卓系统的后台保活机制，确保闹铃和震动在锁屏或App退到后台时能准时触发。

通知栏交互：需利用 Android Notification 的 addAction 功能，实现不打开App直接在通知栏修改时间的交互。

